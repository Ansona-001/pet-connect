# Requirements Traceability Matrix

Seeded from the feature truth table in `PETCONNECT_AI_AGENT_COMPLETION_BRIEF.md` §9, then cross-checked against the actual code in this repository (not assumed) as of Day 2. "Verified" notes below are things confirmed directly against `server/internal/**` and `lib/**` during this pass; anything not marked verified carries the brief's original claim forward unchanged. Update the **Status** and **Evidence** columns as each milestone slice lands — this file is the source of truth the brief's operating contract (§1.3) requires.

Legend — **Status**: `Working core` / `Partial` / `Demo only` / `Schema only` / `Missing`. `Δ` marks a status this pass changed from the brief's original snapshot.

**Milestone 2 close-out (Day 30):** `server/integration/persistence_test.go` closes M2 with cross-user, cross-restart regression coverage rather than a new feature. `TestCrossUserPersistenceAfterRestart` writes a follow/like/comment via one app+pool instance, then reads all of it back — and performs a further unfollow — via three successively fresh `setupTestEnv` instances sharing only the underlying Postgres, proving no read path depends on in-process state. `TestPrivateAccountRestrictionSurvivesRestart` specifically stress-tests that `visibility.ForUser` (no follow exception, gates `GET /users/:userId`) and `visibility.ForPet` (has a per-pet follow exception, gates pet media tabs) stay correctly *different* from each other across a restart — a follower of the owner's pet must still see the account profile as restricted while that same follow unlocks the pet's own posts tab. An initial draft of the second test asserted a follower gets an unrestricted account profile; that was the test's own wrong assumption, not a bug — `getPublicProfile`'s `response.IsSelf || !isPrivate` check has deliberately never had a follow exception, unlike `ForPet`. All 8 subtests across both tests pass; no new production bugs surfaced this day (Days 28–29 already found and fixed the real gaps in this area).

## Identity, auth & account

| Requirement | Status | Schema | API | Flutter client | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Email registration/login | Working core | `users` (000001_init.sql) | `auth.RegisterRoutes` → `POST /auth/register`, `/auth/login` (`server/internal/modules/auth/auth.go:51`) | `lib/features/auth/` | `internal/modules/auth/*_test.go` | No email verification, reset, or abuse controls yet (M1). |
| JWT/refresh/logout | Working core | `refresh_tokens` | `POST /auth/refresh`, `/auth/logout` | `lib/core` Dio interceptor | `internal/modules/auth/*_test.go` | Verified: access JWTs and sockets aren't revoked on logout — no blacklist/short-TTL check exists in `httpx.Authenticate`. |
| Google/Apple sign-in | Missing | — | — | UI explains credentials required | — | Blocked on OAuth credentials (brief §18). |
| Onboarding | Partial | `users`, `pets` | `PATCH /me`, `POST /me/pets` | `lib/features/onboarding/` | none found | Owner patch and pet creation are separate calls, not atomic. |
| Owner profile | Partial Δ | `users.location` (lat/lng) | `GET/PATCH /me`; Day 21 adds `GET /users/:userId` (`internal/modules/profile/profile.go`) — database-backed stats (pet/post/follower/following counts), city/area only (never lat/lng, per ADR 0002), block-aware (404 in either direction), and a private account's non-owner viewers get name/avatar/pet-count only; Day 28 fixes `getPublicProfile`'s `post_count`/`following_count` subqueries, which had no join to `pets` at all and so over-counted posts/follows belonging to a soft-deleted/paused pet forever (pet soft-delete doesn't cascade to `posts`/`follows`) — now filtered to `deleted_at IS NULL AND status = 'active'`, matching `pet_count`/`follower_count`'s own (already-correct) filtering | Day 27 wires the profile screen's stats row (`profile_screen.dart`'s `_ProfileStats`) to this endpoint — the Posts/Followers/Following/Pets numbers shown for the signed-in user's own profile are now real (`SocialController._fetchProfileStats`, refreshed on app load and after any pet add/edit/delete), replacing four literal `const` values (`'128'`/`'12.8K'`/`'486'`/`'2'`) that had no backing data at all | `server/integration/profile_test.go` (live, 6 cases); Day 27's Flutter change verified via `flutter test`/`flutter analyze` plus live curl + DB checks (no dedicated widget test); Day 28's count fix covered by `server/integration/visibility_test.go`'s `TestProfileCountsExcludeDeletedPets` (live) | `GET /me`'s raw `location.latitude/longitude` issue (brief defect #7, `account.go:38-41`) is unchanged — still only `/me`'s own concern, not inherited by the new public endpoint, which was built with its own response type specifically to avoid that reuse (see ADR 0002's own warning about this). |
| Location visibility / discovery participation | Working core Δ | `users.is_discoverable` (`000006_location_discovery_participation.sql`) | Day 29 adds `is_discoverable` alongside `is_private` on `PATCH/GET /me` (`account.go`) — a separate, location-specific "appear in discovery/matching" opt-out that migration 000003's own comment had explicitly reserved for this day. Defaults to `true` (opt-out, not opt-in) so existing users don't silently vanish. `discovery/handler.go`'s `nearbyPets` and `matching/service.go`'s `Candidates` both now require `owner.is_discoverable`, with no follow-based exception (unlike `is_private`) — opting out means opting out even for people already following you | `EditProfileScreen` gets a second `SwitchListTile.adaptive`, "Show me in discovery", mirroring the existing private-profile toggle exactly; threaded through `AuthUser`/`AuthRepository.updateProfile`/`AuthController.updateProfile` | `server/integration/location_discovery_test.go` (live, 3 cases: `PATCH /me` round-trip, discovery exclusion, matching exclusion) | **Live-verified bug found and fixed during this pass**: `/auth/login`'s (and register's/OAuth's) `userResponse` had never been updated when `is_private` was added, so it silently omitted the field from its JSON — a fresh login parsed the missing key as "not explicitly false" and showed the toggle back on, even though `GET /me` and the database both correctly had it `false`. Fixed by adding `is_discoverable` to `userResponse` and all 4 of its SQL construction sites (register, login, and both OAuth paths in `oauth.go`) alongside the pre-existing `is_private` column — confirmed via a fresh Playwright browser session after a real `PATCH /me` that the toggle now correctly reads its true, persisted server value on login rather than a stale default. |
| Multi-pet profiles | Partial Δ | `pets` | `pets.RegisterRoutes` (`pets.go:52-57`) — full CRUD; Day 22 fixes the `pets.location` raw-lat/lng exposure (ADR 0002) — `GET /pets/:petId` now omits `location` entirely for any non-owner viewer via `applyVisibility`, using the same shared `internal/platform/visibility` block/private-account check as the new public pet-profile media tabs (`GET /pets/:petId/{posts,reels,stories}` in `social/handlers.go`, brief Milestone 2's "Public pet profile & media tabs") | partial | `internal/modules/pets/*_test.go` | The **Verified new finding** from Day 2 (raw lat/lng at `pets.go:60-63`) is now fixed for non-owner viewers; the owner's own `GET /pets/:petId` and `/me/pets` responses are intentionally unaffected (exact location is only ever the owner's business). |
| Blocks/mutes/reports | Partial Δ | `blocks`, `mutes`, `reports` (000004_safety_primitives.sql) | `safety.RegisterRoutes` → `POST/GET /blocks`, `DELETE /blocks/:userId`, `POST/GET /mutes`, `DELETE /mutes/:userId`, `POST /reports` (`server/internal/modules/safety/safety.go`) | none yet | none | Live-verified Day 19: feed/discovery/matching-candidates exclude blocked users both directions; feed also excludes muted users (mute doesn't affect discovery/matching); blocking sets any active `matches.status='blocked'`, which the pre-existing chat module already treats as terminal (hides from `GET /chats`, rejects `SendMessage`) — no chat module changes needed. Day 28 closes the gap this row used to flag: `canViewPost` (`social/handlers.go`, the shared gate for like/save/comment/list-comments) previously had **no block check at all** — a blocked user could still like, save, and comment on the blocker's posts — now fixed with the same bidirectional `NOT EXISTS` clause the main feed uses. `listStories` had the same gap plus a worse bug (see the Stories row below); both fixed Day 28. A Flutter UI for blocks/mutes/reports themselves is still not wired. |
| Sessions/devices | Missing | none | none | none | — | `JWT_REFRESH_SECRET` is loaded/validated but unused (opaque refresh tokens) — dead config per brief §6. |

## Social graph & content

| Requirement | Status | Schema | API | Flutter client | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Feed | Partial Δ | `posts`, `post_likes` (+`actor_pet_id`, `000005_reaction_identity_attribution.sql`), `post_saves` | `GET /feed`, `PUT/DELETE .../like`, `.../save` (`social/routes.go:13-20`); Day 26 adds an optional `actor_pet_id` body field to `PUT .../like`, validated by the shared `resolveOwnedActorPet` (400 `invalid_actor_pet_id` on a malformed UUID, 403 `pet_not_owned` if the caller doesn't own it), upserted via `ON CONFLICT (user_id, post_id) DO UPDATE` | `lib/features/social` | none in `social` package (0 test files) | Cursor pagination exists (`timeCursor`, `models.go:97`) but feed ranking/infinite scroll on client is unverified this pass. Day 26's `actor_pet_id` plumbing is server-only so far — no Flutter UI yet to pick which pet is "liking." |
| Video/carousel posts | Schema + upload validation Δ | Day 31 adds `media` (durable, database-tracked upload records — `owner_user_id`, `media_type`, `storage_path`, `content_type`, `byte_size`, `width`/`height`/`duration_ms`) and `post_media` (an ordered `(post_id, media_id, position)` join, `UNIQUE (post_id, position)`, `ON DELETE RESTRICT` on `media_id` so in-use media can't be deleted out from under a post) — `000007_durable_media_schema.sql`. `Post` still has only `media_url`/`media_type`, untouched (ADR 0006, additive-only) | Day 32's `POST /media/uploads` (`media/handler.go`) now inserts a `media` row on every successful upload (returning its `id` alongside the existing `url`/`path`) — closing the gap Day 31 flagged, that no Postgres row ever represented an upload. Also adds magic-byte sniffing (`sniffMatches`) so the file's own leading bytes must match its declared Content-Type — previously the client-supplied header was trusted outright — plus an image-specific byte-size cap (15MB, tighter than the general upload ceiling) and an 8000px max dimension per side, decoded header-only via `image.DecodeConfig`/`golang.org/x/image/webp` (no full pixel buffer allocated) | same as above | `server/internal/modules/media/handler_test.go` (`TestSniffMatches`, `TestAcceptedMedia`, 35 cases, pure unit tests) | **Verified live** (Docker + real ffmpeg-generated JPEG/PNG/WebP/MP4 files): valid uploads of all 3 image formats correctly decode and persist width/height; a valid MP4 persists with null dimensions (video dimension probing is out of scope — no ffprobe-equivalent dependency exists yet); a 9000×100 JPEG is rejected `422 image_too_large`; a 26MB valid JPEG under the dimension limit is rejected `413 invalid_file_size` (the image-specific cap, distinct from the general one); plain text claiming `image/jpeg` and a real MP4 claiming `image/png` are both rejected `415 media_content_mismatch`; the pre-existing `download` endpoint and `POST /posts`' `media_url` flow are both unaffected. `post_media` still has no reader/writer — that remains the "Carousel post API" day's job. |
| Stories | Partial Δ | `stories` | `GET/POST /stories` | simple viewer | `server/integration/visibility_test.go`'s `TestListStoriesEnforcesVisibility` (live, 2 cases) | `Story.TextOverlay` is a stored `map[string]any` — overlay data persists, but no views/reactions/replies tables exist. **Day 28 fixed a real privacy bug**, the most severe finding of the "Visibility enforcement" day's audit: `listStories`' WHERE clause had `OR NOT EXISTS (SELECT 1 FROM follows WHERE user_id = $1)`, which showed *every* active story in the app — including private accounts' — to any viewer following zero pets (e.g. every brand-new user, before their first follow). Removed that clause and added the bidirectional blocks check the main feed already had. |
| Reels | Partial/API-only | reuses `posts` table | `GET/POST /reels` (`social/routes.go:25-26`) | reuses feed UI | none | Confirmed reels are not a distinct schema — same `Post`/`Comment` types, just filtered. |
| Follows | Working core Δ | `follows` table exists in `000001_init.sql` | Day 23 adds `PUT/DELETE /pets/:petId/follow` (`social/handlers.go`'s `setFollow`) — idempotent both directions, block-aware 404 via `visibility.ForPet`, rejects following your own pet, notifies the owner only on actual creation. Day 24 adds `GET /users/:userId/{followers,following}` (`internal/modules/profile/profile.go`) — cursor-paginated with a `total_count` matching the `GET /users/:userId` aggregate, a new shared `visibility.ForUser`. Day 25 adds `followed_by_me` to `Post` (`social/models.go`) so feed/reel responses carry real per-viewer follow state | Day 25 wires `ReelsScreen`'s follow button to the real API — removed the local-only `_followedHandles` `Set<String>`, `SocialController.toggleFollow` now calls `PUT/DELETE /pets/:petId/follow` with optimistic update + revert-on-error (flips every post from that pet, not just the tapped one), the button is hidden entirely on the viewer's own posts | `server/integration/follows_test.go` (live, 6 cases), `server/integration/followers_test.go` (live, 6 cases), `server/integration/persistence_test.go` (live, 2 tests/8 cases, Day 30 — cross-restart consistency) | Live-verified end to end in the actual web UI (Docker + Playwright): follow/unfollow both fire the real request and update the DB (`follows` row + owner `notifications` row), and — the property that actually proves this isn't just optimistic local state — a completely fresh login still shows "Following" because it's now read from the server's `followed_by_me`, not a client-side toggle. |
| Comments | Partial Δ | `comments` (+`actor_pet_id`, `000005_reaction_identity_attribution.sql`) | `GET/POST /posts/:postId/comments`; Day 26 threads ADR 0001's identity-attribution model through both — `POST` accepts an optional `actor_pet_id`, validated by the same `resolveOwnedActorPet` helper `PUT .../like` uses, and both `GET`/`POST` resolve `author_name`/`author_photo_url` via `COALESCE(pet.name, u.name)`/`COALESCE(pet.primary_image_url, u.profile_photo_url)` so a pet-attributed comment displays the pet's identity, not the owner's; Day 28 fixes `canViewPost` (the shared gate behind both routes, plus like/save) to also reject blocked users and posts whose pet was soft-deleted after the post was created | — | live-verified via curl + direct DB checks; Day 28's fix also covered by `server/integration/visibility_test.go`'s `TestCanViewPostEnforcesBlocks`/`TestCanViewPostExcludesDeletedPet` (live) | No edit/delete endpoints exist yet (create + list only). Live-verified: valid owned `actor_pet_id` persists and resolves to the pet's name/photo; malformed UUID → 400 `invalid_actor_pet_id`; unowned pet → 403 `pet_not_owned`; omitted/empty `actor_pet_id` still falls back to the user's own identity, matching ADR 0001's "no data implies no pet attribution." |
| Hashtags/mentions/shares | Missing | none | none | — | — | No parsing, no share endpoint, no hashtag table. |

## Discovery & matching

| Requirement | Status | Schema | API | Flutter client | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Nearby discovery | Partial/API-only Δ | PostGIS query in `discovery` module | `GET /discovery/pets` (`discovery/routes.go:11`); Day 28 adds a private-account exclusion to the `nearby` CTE — a private account's pet is now only discoverable to a viewer who already follows that specific pet, mirroring `internal/platform/visibility.ForPet`'s own exception; Day 29 adds `AND owner.is_discoverable`, unconditionally (no follow exception, unlike `is_private`) | `discover_screen.dart` uses `MockSocialData` | `server/integration/visibility_test.go`'s `TestDiscoveryExcludesPrivateAccounts` (live, 2 cases); Day 29's opt-out covered by `location_discovery_test.go`'s `TestDiscoveryExcludesOptedOutUsers` (live) — the discovery module's first test coverage | **Verified:** `Candidate.DistanceKM` (`discovery/handler.go:34`) is already privacy-safe (rounded distance, not coordinates) — better than the brief's general warning suggested for this specific endpoint. Day 28's audit found `is_private` was never checked at all before this fix — any stranger could discover/search a private account's pets. |
| Pet swipe/match | Working core Δ | `pet_swipes`, `matches`, auto `chats` row | `POST /pets/:petId/swipes`, `GET /matches` (`matching/handler.go:23-25`); Day 27 has the client actually pass `max_distance_km` on `GET /pets/:petId/candidates` (`matching/handler.go:41`, already existed server-side but was never sent); Day 28 adds `AND NOT owner.is_private` to the candidate query — unlike discovery, a swipe candidate is by definition a stranger, so there's no "already following" exception to grant; Day 29 adds `AND owner.is_discoverable` alongside it | match screen; Day 27 wires the "Discovery preferences" sheet's distance slider to a real `SocialState.matchRadiusKm` — the header's `'within N km'` text was a hard-coded `10` regardless of what the slider was set to (or whether "Apply filters" had ever been tapped), and no radius was ever sent to the candidates query at all (every fetch was effectively unbounded) | `internal/modules/matching/*_test.go`; Day 28's fix covered by `server/integration/visibility_test.go`'s `TestMatchingExcludesPrivateAccounts` (live); Day 29's opt-out covered by `location_discovery_test.go`'s `TestMatchingExcludesOptedOutUsers` (live) | **Verified:** swipe already carries a `ClientRequestID` for idempotency (`matching/types.go:63`) — concurrency handling is more built-out than the truth table implies. The pet-type filter chips in the same sheet remain cosmetic-only (not in Day 27's scope — no numeric metric involved). |
| Playdates | Missing | none | none | — | — | No schema at all yet. |
| Search (pet/owner/hashtag) | Missing | none | none | — | — | Confirmed no `/search` route and no OpenSearch/trigram wiring in `cmd/api/main.go`. |

## Chat, notifications, push

| Requirement | Status | Schema | API | Flutter client | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Text chat | Working core | `chats`, `chat_members`, `messages` | `chat.Register` (`chat/handler.go:23-26`) | chat screen | `internal/modules/chat/*_test.go` | **Verified:** messages already carry `ClientMessageID` for idempotent send (`chat/types.go:24,57`) and `MessageType`/`MediaURL` fields exist server-side — schema is ahead of the Flutter client here. |
| Image/video/audio/location chat | Backend partial/client missing | `messages.message_type`, `media_url` columns exist | send accepts any `message_type` string (unvalidated against an enum in the handler I read) | placeholders | — | Backend accepts the fields; nothing enforces which `message_type` values are valid yet. |
| Presence/typing/receipts | Partial | — | `PUT /chats/:chatId/read`; typing/read socket events referenced in brief §6 | not wired | — | Not independently re-verified this pass; carried from brief. |
| In-app notifications | Backend only | `notifications` | `GET /notifications`, `PUT .../read-all`, `PUT .../:id/read` | placeholder/fake | none | **Verified defect, exactly as brief describes:** `listResponse` (`notifications.go:53-57`) has `HasMore bool` but **no `NextCursor` field at all** — `has_more: true` is structurally impossible to page through today. |
| Push notifications | Missing | none | none | none | — | No device/token table, no FCM/APNs wiring. |

## Communities, events, adoption

| Requirement | Status | Schema | API | Flutter client | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| Communities | Backend partial/client mock | `communities`, `community_members` | list/create/join/leave only (`community/routes.go:11-14`) | mock content | 0 test files | No details/roles/discussions/moderation endpoints exist. |
| Events | Backend partial/client mock | `events`, `event_rsvps` | list/create/RSVP only (`events/routes.go:11-14`) | mock state | 0 test files | **Verified new finding:** `Event.Latitude`/`Longitude` (`events/handler.go:33-34`) are returned as raw exact coordinates with no rounding — same class of leak as `/me` and `/pets/:petId`, and arguably higher-risk since events imply a real meeting point. Flag for the M4 location-privacy audit. |
| Adoption | Backend partial/client mock | `adoption_listings`, `adoption_saves` | list/create/save only (`adoption/routes.go:11-14`) | mock state | 0 test files | No organizations, verification, applications, or status history schema yet. |

## Platform, security, ops

| Requirement | Status | Notes |
| --- | --- | --- |
| Media upload/access | Missing hardening | **Verified:** `media.Handler.upload` (`media/handler.go:34-71`) validates only the client-supplied `Content-Type` header against an allowlist — no magic-byte sniffing, no re-encoding, no scanning. **Verified:** `GET /media/uploads/*` (`media/handler.go:73-85`) is mounted on the **public**, unauthenticated router (`h.Register(protected, public)` in `cmd/api/main.go:151` — `download` goes on `public`) — anyone with a URL can fetch any uploaded object today, private or not. This is a concrete instance of brief defect #6/§15 "never authorize private media merely because someone knows a URL," not just a theoretical risk. |
| Rate limiting/abuse controls | Partial Δ | Day 20: `internal/platform/ratelimit` — a Redis-backed fixed-window limiter, applied per-route to `/auth/register` (5/15min), `/login` (10/15min), `/refresh` (60/15min), `/forgot-password`, `/resend-verification` (5/15min each), `/reset-password` (10/15min), `/verify-email` (20/15min), all keyed by IP. Every rejection returns the identical generic `rate_limited` 429 regardless of endpoint or cause (enumeration-safety, brief §13); fails open on a Redis outage (live-verified). Not yet applied to OAuth routes (their own short-lived Redis state/ticket TTLs already throttle implicitly) or to non-auth endpoints (`/reports` explicitly deferred — see safety.go's `createReport` comment). |
| Authorization test matrix (BOLA) | Partial Δ | Day 20: `server/integration/bola_test.go` — a live HTTP test matrix (real Postgres/Redis, skips gracefully when unavailable, e.g. in CI) covering pets patch/delete, session revoke, notification read, matching swipe source-pet ownership, post creation pet ownership, and chat sender_pet_id/membership — all confirmed correctly scoped, live-verified passing. A full route-by-route audit across every module with an ID-scoped route (pets, social, auth/sessions, account, safety, chat, matching, discovery, community, events, adoption, notifications) found no unscoped resource route among those that should be scoped; the one flagged item (public unauthenticated `GET /media/uploads/*`, row above) is ADR 0003's already-recorded, deliberate design boundary, not a new gap. |
| Premium/entitlements/billing | Missing | No schema, no routes. |
| Analytics/telemetry | Missing | No event emission found anywhere in `lib/` or `server/`. |
| Production infra | Missing | `compose.yaml` (local only), no `infra/`, no CI beyond what M0 Day 4 adds. |

## Backend test coverage (verified, Day 1 finding carried here for visibility)

Packages **with** `_test.go` files: `account`, `auth`, `chat`, `matching`, `notifications`, `pets`, `realtime`.
Packages with **zero** test files: `adoption`, `community`, `discovery`, `events`, `media`, `social`. `go test ./...` reports `ok` for these too — they simply have nothing to fail.

## Open items this matrix will track going forward

- Every day's slice in the build planner should update the relevant row's Status/Evidence/Tests columns before that day is marked done.
- Three raw-coordinate exposures are now confirmed (not hypothetical): `GET /me`, `GET /pets/:petId`, and `GET/POST /events`. These need the location-privacy pass (ADR 0002, M1/M4 work) before any of those endpoints are called by production-facing UI beyond the current owner-only screens.
- The unauthenticated `GET /media/uploads/*` route needs to move behind an authorization check (or signed URLs) before any private media type is introduced — currently low-risk only because all uploads are effectively public today, which is itself the gap to close.
